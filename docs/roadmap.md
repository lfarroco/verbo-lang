Tier 1: Immediate Focus (Maximum Impact)
REST Servers
Why perfect first:

Directly leverages your existing route/schema specs

Highly structured domain (CRUD operations)

Natural language excels at describing resources/endpoints

Huge market need (APIs power everything)

Example: Describe User API → Full Oak.js server

CLI Tools
Why ideal second:

Simple I/O patterns map well to natural language

Limited scope reduces ambiguity

Great for automation/batch processing

Demonstrates Verbo's versatility

Example: File converter spec → Python/Deno CLI

Tier 2: Secondary Wave (After MVP)
Admin Dashboards (React subset)
Better than general React apps:

Tabular data presentation is highly structured

Limited interactivity needs (forms, tables, charts)

Can reuse REST API output

Example: "Dashboard showing user stats" → React + Chart.js